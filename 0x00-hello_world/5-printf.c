#include <stdio.h>

/**
 * main - this is the main function that the program will read first
 *
 * Return: this return value is always zero
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
